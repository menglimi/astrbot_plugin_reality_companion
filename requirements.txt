# AstrBot supplies the astrbot package.
aiohttp>=3.10,<4
numpy>=1.26,<3
sounddevice>=0.5,<1
soundfile>=0.12,<1
# AstrBot Desktop already bundles OpenCV. Do not install any cv2 distribution
# here: duplicate binary packages can make OpenCV's loader recurse at startup.
