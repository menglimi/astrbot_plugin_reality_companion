# 手机陪伴终端从零配对说明书

本文适用于“手机陪伴终端”Android App、Reality Companion 移动网关，以及 Together Companion 的一起看/通话/工作协同功能。

先记住一条：**手机只连接 Reality Companion 移动网关，不要直接连接 Together 的 6321 端口。**移动网关默认端口是 `6322`，也可以在 Reality Companion 设置中改成其他端口。

## 一、准备工作

### 1. 安装插件

在 AstrBot 中安装并启用：

- `astrbot_plugin_reality_companion`：提供移动网关、配对、位置和统一房间代理。
- `astrbot_plugin_together_companion`：需要一起看、视频通话或工作协同时安装。
- `astrbot_plugin_game_companion`：需要手机游戏房间时安装。
- `astrbot_plugin_screen_companion`：需要手机屏幕共享状态时安装。

Together 房间还需要配置实时共处对话模型，否则手机虽然能配对，房间能力仍会显示未就绪。

### 2. 选择 Android 安装包

- 只需要配对、一起看和游戏：`app-lanRelease.apk`。
- 需要位置共享：`app-locationLanRelease.apk`，这是常规推荐包。
- 需要位置但要兼容部分安全检测：`app-fullLanRelease.apk`。
- 使用公网 HTTPS：可使用 `app-release.apk`，也可以继续使用 LAN 包。

`lanRelease` 和 `locationLanRelease` 允许可信局域网/组网地址使用 HTTP；普通 `release` 只允许 HTTPS/WSS。公网地址不要使用 HTTP。

### 3. 准备配对信息

在 Reality Companion 的移动网关设置中填写：

```yaml
mobile:
  enabled: true
  host: 0.0.0.0
  port: 6322
  public_base_url: "https://companion.example.com"
  pairing_token: 请填写至少 24 位随机长令牌
  allowed_user_id: 你的 AstrBot 用户 ID
  proxy_rooms: true
```

说明：

- `0.0.0.0` 是服务端监听占位地址，不是手机端填写的地址。
- 手机端应填写电脑实际可访问的局域网 IP、组网 IP 或公网域名。
- 使用 Docker + Caddy/Nginx 时，将 `public_base_url` 设置为手机实际访问的完整 HTTPS 地址。它用于生成一起看、视频通话和游戏房间链接，避免把 Docker 容器内网 IP 返回给手机。
- `proxy_rooms` 建议保持开启。它会把一起房间的页面、接口、媒体和 WebSocket 统一转发到移动网关。
- 端口可以自定义，例如改为 `16322` 后，手机地址就使用 `http://电脑IP:16322` 或 `https://域名`。

保存配置后重启 Reality Companion 或 AstrBot。只保存不重启时，旧网关可能仍在监听旧地址。

### 4. 获取配对令牌

用允许配对的用户私聊 Bot 发送：

```text
现实触及 配对令牌
```

令牌不要发群聊、不要截图公开。需要换新令牌时发送：

```text
现实触及 重置配对令牌
```

重置后，旧手机会话失效，需要重新配对。

## 二、路线 A：同一局域网

这是最简单的现场连接方式，适合电脑和手机连接同一个 Wi-Fi 或网线局域网。

### 服务端

1. `mobile.enabled` 开启。
2. `mobile.host` 可以填写电脑局域网 IP，也可以保留 `0.0.0.0`。
3. 端口默认 `6322`。
4. `proxy_rooms` 保持开启。
5. Windows 防火墙允许该端口的局域网入站连接。

### 手机端

在 App 的“设置 → 移动网关”填写：

```text
http://电脑局域网IP:6322
```

例如：

```text
http://192.168.1.20:6322
```

然后填写配对令牌并配对。

### 连通性检查

先用手机浏览器打开：

```text
http://192.168.1.20:6322/health
```

能返回 JSON，说明网关端口可达。打不开时优先检查电脑防火墙、手机是否连到了访客 Wi-Fi，以及电脑 IP 是否发生变化。

## 三、路线 B：Tailscale / ZeroTier 组网

这是长期使用最推荐的方式。它比公网端口转发简单，也比临时隧道稳定。

### 操作步骤

1. 电脑和手机安装同一个组网工具，并登录同一网络。
2. 记下电脑的组网 IP，例如 Tailscale 常见的 `100.x.x.x`。
3. Reality Companion 网关监听地址可填写该组网 IP，也可以保留 `0.0.0.0`。
4. 手机填写：

```text
http://电脑组网IP:6322
```

例如：

```text
http://100.66.1.4:6322
```

5. 使用 `lanRelease` 或 `locationLanRelease` 包完成配对。

组网链路本身负责加密，手机不需要直连 Together 的 `6321`。统一房间代理开启后，Together 和 Game Companion 只需要在电脑本机可达。

## 四、路线 C：传统内网穿透

适合没有 Tailscale/ZeroTier，或需要让外部设备通过一个固定地址访问的情况。可使用 frp、ngrok、Sakura Frp 等支持 HTTPS 和 WebSocket 的方案。

### 推荐拓扑

```text
手机 → HTTPS 内网穿透地址 → 电脑 127.0.0.1:6322 → Reality Companion
```

穿透工具的本地目标建议指向：

```text
http://127.0.0.1:6322
```

不要把 Together 的 `6321` 作为手机端目标。手机端填写穿透服务提供的 HTTPS 地址，例如：

```text
https://your-tunnel.example.com
```

### 必须满足

- 穿透服务支持 WebSocket。
- 动态接口不能被缓存。
- 不要在 `/health`、`/pair`、`/status`、`/room/*` 前增加需要浏览器 Cookie 的登录页。
- 穿透地址必须稳定；临时地址变化后需要在 App 设置中更新。
- 只暴露移动网关，不要同时暴露 AstrBot Dashboard、Together `6321` 或其他管理端口。

## 五、路线 D：公网固定地址 + HTTPS 反向代理

这是适合长期公网访问的正式方案。需要一个域名、证书和公网入口。

### 推荐拓扑

```text
手机 → https://companion.example.com → Caddy/Nginx → 127.0.0.1:6322
```

Reality Companion 可以只监听 `127.0.0.1`，由 Caddy/Nginx 负责 TLS。手机 App 填写：

```text
https://companion.example.com
```

不需要在地址后填写 `6322`，除非反向代理明确使用了其他公网端口。

### Docker + Caddy 示例

```yaml
mobile:
  enabled: true
  host: 0.0.0.0
  port: 6322
  public_base_url: "https://companion.example.com"
  proxy_rooms: true
```

Caddy 需要保留外部请求信息：

```text
header_up Host {host}
header_up X-Forwarded-Host {host}
header_up X-Forwarded-Proto {scheme}
```

配置了 `public_base_url` 后它优先级最高；未配置时，网关依次读取 `X-Forwarded-Host`、`Host` 和 `X-Forwarded-Proto`，最后才回退到本地监听地址。

### 反向代理要求

- 转发所有 HTTP 方法和请求体。
- 转发 WebSocket Upgrade/Connection 头。
- 关闭动态接口缓存。
- 保留较长的 WebSocket 读取超时。
- 不要把管理后台登录页注入 `/pair`、`/status`、`/room` 等接口。
- 只开放 HTTPS 入口，防火墙不要把 `6322` 直接暴露到公网。

浏览器通话场景必须使用 HTTPS，否则浏览器不会允许摄像头和麦克风。Android 原生 LAN 包在可信私有网络中可以使用 HTTP，但公网仍应使用 HTTPS。

## 六、路线 E：Cloudflare Tunnel / CF 代理

Cloudflare 适合没有公网 IP、希望使用 HTTPS 域名的场景。推荐使用已绑定域名的 Tunnel，不建议长期依赖临时 Quick Tunnel 地址。

### 方案 1：Cloudflare Tunnel

在电脑上让 `cloudflared` 把域名转发到本机网关：

```text
https://companion.example.com → http://127.0.0.1:6322
```

手机端填写：

```text
https://companion.example.com
```

Cloudflare Tunnel 必须允许 WebSocket，且不能给移动网关接口额外套一个需要登录 Cookie 的 Access 页面。配对令牌本身已经是认证凭据，建议使用防火墙、Tunnel 私网和强令牌保护入口。

### 方案 2：Cloudflare DNS 橙云代理

如果电脑本身有公网入口，可以让 `companion.example.com` 代理到 Caddy/Nginx 的 HTTPS 服务，再由反向代理转到 `127.0.0.1:6322`。不要让 Cloudflare 直接代理一个没有 TLS 的公网 `6322` 端口。

### Quick Tunnel 注意事项

`cloudflared tunnel --url http://127.0.0.1:6322` 生成的 `trycloudflare.com` 地址可能变化，适合临时测试，不适合长期配对。地址变化后，App 必须重新填写地址；如果旧地址仍能打开缓存页面，但 WebSocket 指向旧域名，就会出现“一起功能一直连接中”。

## 七、完成配对后的检查

1. App 设置页显示“已连接”。
2. 首页状态显示“共同房间可用”。
3. 打开“一起”页，创建一次通话或一起看房间。
4. 手机打开的房间 URL 主机应仍是移动网关域名/IP，不应跳转到 `6321` 或 `127.0.0.1`。
5. 需要位置时，在位置页单独授予位置权限并打开前台共享；`lanRelease` 不包含位置能力，必须安装 `locationLanRelease` 或 `fullLanRelease`。

## 八、常见问题

### 能打开地址，但配对失败

检查配对令牌、`allowed_user_id`、网关是否重启，以及手机是否访问了正确端口。`/health` 能打开只代表端口可达，不代表配对权限正确。

### 已配对，但一起功能一直显示“正在连接”

按顺序检查：

1. 手机是否使用移动网关地址，而不是 Together `6321` 直连地址。
2. `mobile.proxy_rooms` 是否开启。
3. Together 是否启用并正在运行。
4. Together 是否配置实时共处对话模型。
5. 穿透或 CF 代理是否支持 WebSocket，是否把房间页面、`/ws`、`/media` 一起转发。
6. 修改端口、代理或域名后是否重启网关并重新创建房间。

### 公网地址能打开，但视频通话创建失败

浏览器通话需要 HTTPS。检查手机是否使用 `https://`，反向代理是否有有效证书，Together 是否返回安全房间地址。Android 原生 LAN 通话的 HTTP 例外只适用于私有局域网/组网地址，不适用于公网 IP。

### 位置功能没有出现

确认安装的是 `locationLanRelease` 或 `fullLanRelease`，而不是不带位置能力的 `lanRelease`；同时在 Android 系统设置中授予位置权限。

### 电脑重启后又要配对

移动会话只保存在服务端内存中，AstrBot 重启后会失效。App 会尝试用本地加密保存的配对令牌自动重配；如果配对令牌已重置，则需要重新输入新令牌。

## 九、安全建议

- 配对令牌至少 24 位随机字符，不要发群聊或截图公开。
- 公网场景只使用 HTTPS/WSS，不要直接暴露 `6322` 的 HTTP 服务。
- 不要把 AstrBot Dashboard、Together `6321`、Screen Companion 端口直接暴露到公网。
- `proxy_rooms` 保持开启，减少手机直连本机服务端口的需求。
- Cloudflare Access、Basic Auth 等额外认证应谨慎使用；如果客户端无法携带对应 Cookie/Authorization，会表现为页面能打开但接口或 WebSocket一直连接。
- 定期轮换配对令牌，轮换后关闭旧会话并重新配对。
